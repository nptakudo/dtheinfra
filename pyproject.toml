[project]
name = "dtheinfra"
version = "0.1.0"
description = "End-to-end data platform with Lambda architecture"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Data Platform Team" }]
dependencies = []

[tool.uv.workspace]
members = [
    "platform/datagen",
]
